import React from 'react';
import { Container, Typography } from '@mui/material';

const CustomComponent = () => {
  return (
    <Container>
      <Typography variant="h4">Otro Item</Typography>
      <Typography variant="body1">Contenido para el otro item.</Typography>
    </Container>
  );
};

export default CustomComponent;
