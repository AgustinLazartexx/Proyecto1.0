import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  container: {
    marginTop: '2rem',
  },
  card: {
    marginBottom: '1rem',
  },
});

export default useStyles;
